# Temporarily disabled unless we handle the deadlock in ocular
config.unsupported = True

import lit.formats

config.name = "iGoat test suite"
config.test_format = lit.formats.ShTest("0")

config.suffixes = ['.scala' ]
